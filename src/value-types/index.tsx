import { ProRenderFieldPropsType } from '@ant-design/pro-components';
import { tags } from './tags';

/**
 * 扩展类型
 */
export type ValueType = 'tags';

/**
 * 扩展值映
 */
export const valueTypeMap: Record<string, ProRenderFieldPropsType> = {
  tags,
};
